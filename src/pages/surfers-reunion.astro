---
import BaseLayout from '../layouts/BaseLayout.astro';
import ScrollWaves from '../components/ScrollWaves.astro';
import WaveSurfer from '../components/WaveSurfer.astro';
import PirateShip from '../components/PirateShip.astro';
import Lighthouse from '../components/Lighthouse.astro';
---

<BaseLayout
	title="Surfers Reunion"
	description="A gathering of generations—Del Mar surfers past and present, together again."
	pageKey="surfers-reunion"
>
	<section class="hero hero-sunset-bg">
		<div class="hero-content hero-entrance">
			<h1 class="hero-title" data-editable="reunion.hero.title">SURFERS REUNION</h1>
			<p class="tagline hero-tagline" data-editable="reunion.hero.tagline">A Gathering of Generations</p>
		</div>
	</section>
	<div class="wave-wrap wave-hero hero-sunset-bg" id="reunion-wave-wrap">
		<ScrollWaves variant="light" />
		<WaveSurfer />
		<PirateShip />
		<Lighthouse toggleTargetId="reunion-wave-wrap" />
	</div>

	<section class="reunion-content">
		<div class="container">
			<div class="reunion-intro-body">
				<p class="reunion-intro" data-editable="reunion.intro1">
					For decades, surfers have gathered along our local beaches to ride the waves, and share stories, building a unique coastal culture. SURF DEL MAR FESTIVAL brings these generations together again.
				</p>
				<div class="reunion-img-wrap">
					<img src="/surferreunion1.png" alt="Surfers Reunion" class="reunion-section-img" width="1136" height="752" />
					<div class="tape-element"></div>
				</div>
				<div class="reunion-img-wrap">
					<img src="/surferreunion5.png" alt="Surfers Reunion" class="reunion-section-img" width="1228" height="746" />
					<div class="tape-element"></div>
				</div>
				<p class="reunion-intro">
					From the pioneers who surfed here in the 1930s and into the 1950s and 60s, to today's surfers and young riders, this reunion invites everyone connected to Del Mar and the surrounding surf communities to come together and celebrate.
				</p>
				<div class="reunion-img-wrap">
					<img src="/surferreunion2.png" alt="Surfers Reunion" class="reunion-section-img" width="1330" height="748" />
					<div class="tape-element"></div>
				</div>
				<div class="reunion-img-wrap">
					<img src="/surferreunion3.png" alt="Surfers Reunion" class="reunion-section-img" width="1212" height="752" />
					<div class="tape-element"></div>
				</div>
			</div>

			<h2 class="reunion-subhead" data-editable="reunion.subhead1">REMEMBERING THE WAVES</h2>
			<p class="reunion-lead" data-editable="reunion.lead">Throughout the SURF DEL MAR FESTIVAL weekend, reunion events will include;</p>
			<ul class="reunion-list">
				<li data-editable="reunion.list.1">Informal gatherings and storytelling with surfers from different eras</li>
				<li data-editable="reunion.list.2">Historic photographs and shared memories from Del Mar's early surf days</li>
				<li data-editable="reunion.list.3">Film screenings celebrating classic surf culture</li>
				<li data-editable="reunion.list.4">Music and community events bringing surfers, families and friends together</li>
			</ul>
			<article class="event-card">
				<p class="reunion-body" data-editable="reunion.body1">
					The reunion is about reconnecting with old friends, meeting new ones, and honoring the shared love of the ocean that has defined Del Mar and its neighbors for generations.
				</p>
			</article>
			<div class="reunion-img-wrap">
				<img src="/surferreunion4.jpg" alt="Surfers Reunion" class="reunion-section-img" width="3600" height="2448" />
				<div class="tape-element"></div>
			</div>
		</div>

		<div class="doyle-shell">
			<article class="doyle-remember" aria-labelledby="doyle-remember-heading">
				<figure class="doyle-figure">
					<div class="doyle-img-wrap">
						<img
							src="/mike-doyle-larry-brooks.png"
							alt="Del Mar Historical Society President Larry Brooks with legendary surfer Mike Doyle"
							class="doyle-photo"
							width="1024"
							height="683"
						/>
						<div class="tape-element" aria-hidden="true"></div>
					</div>
					<figcaption class="doyle-caption">
						Del Mar Historical Society President Larry Brooks with legendary surfer Mike Doyle.
					</figcaption>
				</figure>

				<div class="doyle-heading-block">
					<h2 id="doyle-remember-heading" class="doyle-title">I Remember<br />Mike Doyle</h2>
					<p class="doyle-subtitle">World Champion surfer — 1969 — Lima, Peru</p>
				</div>

				<div class="doyle-body">
					<p>
						I grew up in L.A. Both of my parents worked in the aircraft business, my mother at Northrop and my father at Longdon Aluminum Forging. In the early 50's we would take trips to the San Diego Zoo and T.J. I would always remember three things, the oil wells in Huntington Beach, the Date Shake Place in Laguna and the piece of beach in Del Mar they now call Dog Beach, where they used to work the race horses in the surf. I said then at the tender age of 12 I would live there someday.
					</p>
					<p>
						In the very early 60s, Bill Bahne and Tony Chanan both surfboard builders had their manufacturing shops in the old Del Mar power house, and I worked for them before getting together with Don Hansen. I loved the surf break there and I was friends with Gardner Stevens, the Del Mar Lifeguard Captain. I knew him when he worked for L.A. City Lifeguards, and I worked for Santa Monica. He asked me to come down to Del Mar and work for him but a deal with Don Hansen sounded more like my style.
					</p>
					<p>
						In 1961 Don Hansen and I with the help of Del Mar resident Bob Driver opened the first surf shop in North County, Hansen Surfboards. Hansen was the organizer, Bob was the money, and I was the famous talent. I came from L.A. and North County was like a Hawaiian California for me. No people and great surf. I moved to Del Mar back behind the beach hill and up against the north slope of the Del Mar Hill. I loved it there. The water in those early 60's went low and left the tidal flats flat as a pancake. We set up a motorcycle flat track there and had weekend friendly races. This is before the freeway. We had celebs like Bruce Brown, the Hoffman brothers and Steve McQueen. The Hansen Surfboards was where the present Kraken Bar is and our manufacturing was right next door across from the present Chart House. Across the street where the Chart house is was the Proving Grounds, where if you wanted to make the Hansen Surf Team you had to go out and show your stuff. It was a golden time period for me coming from L.A. and having miles of open, not-crowded surfing. To me the special spots now and in that era were La Jolla, Del Mar, Laguna Beach, Malibu and Santa Barbara. It's still as beautiful as ever just more people. My slogan was "evolve or dissolve" so I moved to the tip of Baja and saw the whole program do it again.
					</p>
					<p class="doyle-signoff">Mike Doyle</p>
				</div>
			</article>
		</div>

		<div class="container">
			<h2 class="reunion-subhead" data-editable="reunion.subhead2">BE PART OF THE REUNION:</h2>
			<article class="event-card">
				<p class="reunion-body" data-editable="reunion.body2">
					Just like the Beach Boys song says, "You'll catch 'em surfin' at Del Mar," whether you surfed these waves decades ago or just recently discovered them, SURF DEL MAR FESTIVAL welcomes you! Come celebrate the history, the stories and the community that grew from the surf at Del Mar.
				</p>
			</article>

			<article class="event-card event-card-cta">
				<h2 class="reunion-cta-heading" data-editable="reunion.cta-heading">DO YOU HAVE A DEL MAR SURF STORY? SHARE IT WITH EVERYONE!</h2>
				<p class="reunion-cta-body" data-editable="reunion.cta-body">
					Send us your photos, your memories and names of people we should invite. We'll include them in the SHARED MEMORIES section below.
				</p>
				<button type="button" class="reunion-share-btn" id="reunion-share-memories-btn">Share your memories</button>
			</article>

			<h2 class="reunion-subhead reunion-memories-heading">SHARED MEMORIES</h2>
		</div>

		<div id="shared-memories-container" class="shared-memories-container" aria-live="polite">
			<p id="shared-memories-loading" class="shared-memories-msg">Loading…</p>
		</div>
	</section>

	<!-- Share memories modal -->
	<div id="share-memories-modal" class="share-memories-modal" role="dialog" aria-labelledby="share-memories-title" aria-modal="true" hidden>
		<div class="share-memories-overlay" id="share-memories-overlay"></div>
		<div class="share-memories-box">
			<h2 id="share-memories-title" class="share-memories-title">Share your memories</h2>
			<form id="share-memories-form" class="share-memories-form">
				<label for="memory-photo" class="share-memories-label">Photo</label>
				<input type="file" id="memory-photo" name="photo" accept="image/*" required class="share-memories-file" />
				<label for="memory-caption" class="share-memories-label">Your memory (optional)</label>
				<textarea id="memory-caption" name="caption" class="share-memories-textarea" rows="3" placeholder="Tell us about this photo…" maxlength="1000"></textarea>
				<label for="memory-name" class="share-memories-label">Your name (optional)</label>
				<input type="text" id="memory-name" name="name" class="share-memories-input" placeholder="Name" maxlength="200" />
				<p id="share-memories-error" class="share-memories-error" aria-live="polite" hidden></p>
				<p id="share-memories-success" class="share-memories-success" aria-live="polite" hidden>Thank you! Your memory has been added.</p>
				<div class="share-memories-actions">
					<button type="button" class="share-memories-btn share-memories-cancel" id="share-memories-cancel">Cancel</button>
					<button type="submit" class="share-memories-btn share-memories-submit" id="share-memories-submit">Submit</button>
				</div>
			</form>
		</div>
	</div>

	<script>
		import { getSharedMemories, getMemoryComments } from '../lib/firebase';
		import type { SharedMemory, MemoryComment } from '../lib/firebase';

		const SUBMIT_URL      = '/.netlify/functions/submit-memory';
		const COMMENT_URL     = '/.netlify/functions/add-comment';
		const MAX_IMAGE_SIZE  = 700 * 1024;
		const MAX_DIMENSION   = 1200;

		// Share modal refs
		const openBtn      = document.getElementById('reunion-share-memories-btn');
		const shareModal   = document.getElementById('share-memories-modal');
		const shareOverlay = document.getElementById('share-memories-overlay');
		const form         = document.getElementById('share-memories-form') as HTMLFormElement | null;
		const cancelBtn    = document.getElementById('share-memories-cancel');
		const fileInput    = document.getElementById('memory-photo') as HTMLInputElement;
		const errorEl      = document.getElementById('share-memories-error');
		const successEl    = document.getElementById('share-memories-success');
		const containerEl  = document.getElementById('shared-memories-container');
		const loadingEl    = document.getElementById('shared-memories-loading');

		let memoriesList: SharedMemory[] = [];
		let currentIndex = 0;
		let isAnimating  = false;

		// Per-memory comments cache
		const commentsCache = new Map<string, MemoryComment[]>();

		// ── Share modal ───────────────────────────────────────────
		function openShareModal() {
			if (shareModal)  shareModal.hidden = false;
			if (errorEl)    { errorEl.hidden = true; errorEl.textContent = ''; }
			if (successEl)   successEl.hidden = true;
			if (form)        form.reset();
		}
		function closeShareModal() { if (shareModal) shareModal.hidden = true; }

		openBtn?.addEventListener('click', openShareModal);
		shareOverlay?.addEventListener('click', closeShareModal);
		cancelBtn?.addEventListener('click', closeShareModal);

		// ── HTML helpers ──────────────────────────────────────────
		function esc(s: string): string {
			const d = document.createElement('div');
			d.textContent = s;
			return d.innerHTML;
		}

		function slideHTML(idx: number): string {
			const m     = memoriesList[idx];
			const total = memoriesList.length;

			return `
<div class="reel-slide" data-index="${idx}" data-memory-id="${esc(m.id)}">
  <img class="reel-photo" src="${esc(m.imageUrl)}" alt="Shared memory ${idx + 1} of ${total}" />
  <div class="reel-gradient" aria-hidden="true"></div>

  <div class="reel-info">
    <p class="reel-username">🏄 <strong>surf_del_mar</strong></p>
    ${m.caption ? `<p class="reel-caption">${esc(m.caption)}</p>` : ''}
    ${m.name    ? `<p class="reel-by">&#x2014; ${esc(m.name)}</p>` : ''}
    <p class="reel-swipe-hint" aria-hidden="true">↑ swipe for next</p>
  </div>

  <div class="reel-sidebar">
    <button type="button" class="reel-cmt-btn" data-action="toggle-comments" data-memory-id="${esc(m.id)}" aria-label="Comments">
      <svg viewBox="0 0 24 24" width="30" height="30" fill="none" stroke="#fff" stroke-width="1.75">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
      </svg>
      <span id="reel-cmt-count-${esc(m.id)}">…</span>
    </button>
    <a class="reel-save-btn" href="${esc(m.imageUrl)}" download="surf-del-mar-memory-${idx + 1}.jpg" aria-label="Save photo">
      <svg viewBox="0 0 24 24" width="28" height="28" fill="none" stroke="#fff" stroke-width="1.75">
        <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
        <polyline points="7 10 12 15 17 10"/>
        <line x1="12" y1="15" x2="12" y2="3"/>
      </svg>
      <span>Save</span>
    </a>
    ${total > 1 ? `<div class="reel-counter">${idx + 1}<span>/${total}</span></div>` : ''}
  </div>

  <div class="reel-comments-panel" id="tt-comments-section-${esc(m.id)}">
    <button class="reel-cmts-backdrop" data-action="toggle-comments" data-memory-id="${esc(m.id)}" aria-label="Close comments"></button>
    <div class="reel-cmts-sheet">
      <div class="reel-cmts-handle" aria-hidden="true"></div>
      <div class="reel-cmts-header">
        <span class="reel-cmts-title">Comments <span class="tt-comment-count" id="tt-comment-count-${esc(m.id)}"></span></span>
        <button class="reel-cmts-close" data-action="toggle-comments" data-memory-id="${esc(m.id)}" aria-label="Close">&#x2715;</button>
      </div>
      <div class="tt-comments-list" id="tt-comments-list-${esc(m.id)}">
        <p class="tt-comment-placeholder">Loading…</p>
      </div>
      <form class="reel-cmt-form" id="tt-comment-form" data-memory-id="${esc(m.id)}" novalidate>
        <div class="reel-cmt-row">
          <input type="text" id="tt-cmt-text" class="reel-cmt-input" placeholder="Add a comment…" maxlength="500" />
          <button type="submit" class="ig-cmt-post">Post</button>
        </div>
        <input type="text" id="tt-cmt-name" class="reel-name-input" placeholder="Your name (optional)" maxlength="100" autocomplete="name" />
        <p id="tt-cmt-error" class="reel-cmt-error" hidden></p>
      </form>
    </div>
  </div>
</div>`;
		}

		// ── Comments ──────────────────────────────────────────────
		async function loadAndRenderComments(memoryId: string) {
			const listEl  = document.getElementById(`tt-comments-list-${memoryId}`);
			const countEl = document.getElementById(`tt-comment-count-${memoryId}`);
			if (!listEl) return;

			if (!commentsCache.has(memoryId)) {
				const comments = await getMemoryComments(memoryId);
				commentsCache.set(memoryId, comments);
			}
			renderCommentsList(memoryId, listEl, countEl);
		}

		function renderCommentsList(memoryId: string, listEl: Element, countEl: Element | null) {
			const comments = commentsCache.get(memoryId) || [];
			const n = comments.length;
			if (countEl) countEl.textContent = `(${n})`;
			const sidebarCount = document.getElementById(`reel-cmt-count-${memoryId}`);
			if (sidebarCount) sidebarCount.textContent = String(n);
			if (n === 0) {
				listEl.innerHTML = '<p class="tt-comment-placeholder">No comments yet — be the first!</p>';
				return;
			}
			listEl.innerHTML = comments.map(c => `
				<div class="tt-comment">
					<span class="tt-cmt-name">${c.name ? esc(c.name) : 'Anonymous'}</span>
					<span class="tt-cmt-text">${esc(c.text)}</span>
				</div>
			`).join('');
			listEl.scrollTop = listEl.scrollHeight;
		}

		function toggleComments(memoryId: string) {
			const section = document.getElementById(`tt-comments-section-${memoryId}`);
			if (!section) return;
			const opening = !section.classList.contains('open');
			section.classList.toggle('open');
			if (opening) loadAndRenderComments(memoryId);
		}

		// ── Attach event listeners to current slide ───────────────
		function attachSlideListeners() {
			const card = document.getElementById('tt-card');
			if (!card) return;

			card.onclick = (e) => {
				const target = e.target as Element | null;
				if (!target) return;
				const cmtBtn = target.closest('[data-action="toggle-comments"]') as HTMLElement | null;
				if (cmtBtn) {
					e.preventDefault();
					e.stopPropagation();
					toggleComments(cmtBtn.dataset.memoryId || '');
				}
			};

			// Comment form
			const commentForm = document.getElementById('tt-comment-form') as HTMLFormElement | null;
			if (commentForm) {
				commentForm.onsubmit = async (e) => {
				e.preventDefault();
				const memId  = commentForm.dataset.memoryId!;
				const textEl = document.getElementById('tt-cmt-text') as HTMLInputElement;
				const nameEl = document.getElementById('tt-cmt-name') as HTMLInputElement;
				const errEl  = document.getElementById('tt-cmt-error');
				const text   = textEl.value.trim();
				const name   = nameEl.value.trim();
				if (!text) { textEl.focus(); return; }

				const postBtn = commentForm.querySelector('.ig-cmt-post') as HTMLButtonElement;
				postBtn.disabled = true;
				postBtn.textContent = '…';
				if (errEl) errEl.hidden = true;

				try {
					const res = await fetch(COMMENT_URL, {
						method: 'POST',
						headers: { 'Content-Type': 'application/json' },
						body: JSON.stringify({ memoryId: memId, text, name }),
					});
					if (!res.ok) throw new Error('Post failed');

					// Optimistically add to cache and re-render
					const cached = commentsCache.get(memId) ?? [];
					cached.push({ id: Date.now().toString(), text, name, submittedAt: null });
					commentsCache.set(memId, cached);
					const listEl  = document.getElementById(`tt-comments-list-${memId}`);
					const countEl = document.getElementById(`tt-comment-count-${memId}`);
					if (listEl) renderCommentsList(memId, listEl, countEl);
					textEl.value = '';
				} catch {
					if (errEl) { errEl.textContent = 'Failed to post. Try again.'; errEl.hidden = false; }
				} finally {
					postBtn.disabled = false;
					postBtn.textContent = 'Post';
				}
				};
			}

			// Vertical swipe on the whole slide
			const slide = card.querySelector('.reel-slide') as HTMLElement | null;
			if (slide) {
				let touchY = 0;
				slide.ontouchstart = (e) => { touchY = e.changedTouches[0].clientY; };
				slide.ontouchend = (e) => {
					const diff = touchY - e.changedTouches[0].clientY;
					if (Math.abs(diff) > 50) goTo(currentIndex + (diff > 0 ? 1 : -1), diff > 0 ? 'left' : 'right');
				};
			}
		}

		// ── Navigation ────────────────────────────────────────────
		function updateNavButtons() {
			const backBtn = document.getElementById('reel-nav-back') as HTMLButtonElement | null;
			const nextBtn = document.getElementById('reel-nav-next') as HTMLButtonElement | null;
			const navEl = document.getElementById('reel-nav');
			if (!backBtn || !nextBtn || !navEl) return;
			const total = memoriesList.length;
			navEl.hidden = total <= 1;
			backBtn.disabled = currentIndex <= 0;
			nextBtn.disabled = currentIndex >= total - 1;
		}

		function attachNavListeners() {
			document.getElementById('reel-nav-back')?.addEventListener('click', () => goTo(currentIndex - 1, 'right'));
			document.getElementById('reel-nav-next')?.addEventListener('click', () => goTo(currentIndex + 1, 'left'));
		}

		function updateDots() {
			document.querySelectorAll('.tt-dot').forEach((d, i) => d.classList.toggle('active', i === currentIndex));
		}

		async function goTo(newIdx: number, dir: 'left' | 'right') {
			if (isAnimating || newIdx < 0 || newIdx >= memoriesList.length) return;
			isAnimating = true;

			const stage = document.getElementById('tt-stage');
			if (!stage) { isAnimating = false; return; }

			// Build incoming slide: 'left' = next = comes from bottom; 'right' = prev = comes from top
			const tmp = document.createElement('div');
			tmp.innerHTML = slideHTML(newIdx).trim();
			const inSlide = tmp.firstElementChild as HTMLElement;
			Object.assign(inSlide.style, {
				position: 'absolute', inset: '0',
				transform: dir === 'left' ? 'translateY(100%)' : 'translateY(-100%)',
				transition: 'none', zIndex: '2',
			});
			stage.appendChild(inSlide);
			inSlide.offsetHeight;

			const outSlide = stage.querySelector('.reel-slide:not(:last-child)') as HTMLElement | null;
			const ty = dir === 'left' ? '-100%' : '100%';
			const ease = 'transform 0.42s cubic-bezier(0.4,0,0.2,1)';
			inSlide.style.transition = ease;
			inSlide.style.transform  = 'translateY(0)';
			if (outSlide) { outSlide.style.transition = ease; outSlide.style.transform = `translateY(${ty})`; }

			await new Promise<void>((r) => setTimeout(r, 440));

			stage.innerHTML = slideHTML(newIdx).trim();
			currentIndex = newIdx;
			attachSlideListeners();
			updateDots();
			updateNavButtons();
			loadAndRenderComments(memoriesList[newIdx].id);
			isAnimating = false;
		}

		// ── Render feed ───────────────────────────────────────────
		function renderFeed() {
			if (!containerEl) return;
			const showNav = memoriesList.length > 1;
			containerEl.innerHTML = `
<div class="tt-outer">
  <div class="tt-feed">
    <div class="tt-card" id="tt-card">
      <div class="tt-stage" id="tt-stage">
        ${slideHTML(0).trim()}
      </div>
    </div>
    <div class="reel-nav" id="reel-nav"${showNav ? '' : ' hidden'}>
      <button type="button" class="reel-nav-btn" id="reel-nav-back" aria-label="Previous memory" disabled>Back</button>
      <button type="button" class="reel-nav-btn" id="reel-nav-next" aria-label="Next memory">Next</button>
    </div>
  </div>
</div>`;

			attachSlideListeners();
			attachNavListeners();
			updateNavButtons();
			loadAndRenderComments(memoriesList[0].id);

			document.addEventListener('keydown', (e) => {
				if (document.activeElement?.tagName === 'INPUT' || document.activeElement?.tagName === 'TEXTAREA') return;
				if (e.key === 'ArrowDown' || e.key === 'ArrowRight') goTo(currentIndex + 1, 'left');
				if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  goTo(currentIndex - 1, 'right');
			});
		}

		async function renderMemories() {
			if (!containerEl) return;
			const result = await getSharedMemories();
			if (loadingEl) loadingEl.remove();
			if (!result.ok) {
				containerEl.innerHTML = `<p class="shared-memories-msg shared-memories-err">Couldn't load memories. ${esc(result.error)}</p>`;
				return;
			}
			memoriesList = result.memories.filter((m) => m.imageUrl && m.imageUrl.startsWith('data:') && m.approved !== false);
			if (memoriesList.length === 0) {
				containerEl.innerHTML = '<p class="shared-memories-msg">No memories yet — be the first to share!</p>';
				return;
			}
			currentIndex = 0;
			renderFeed();
		}

		// ── Submit new memory form ────────────────────────────────
		form?.addEventListener('submit', async (e) => {
			e.preventDefault();
			if (!errorEl || !successEl || !fileInput) return;
			errorEl.hidden  = true;
			successEl.hidden = true;
			const file = fileInput.files?.[0];
			if (!file) { errorEl.textContent = 'Please choose a photo.'; errorEl.hidden = false; return; }
			const caption = (document.getElementById('memory-caption') as HTMLTextAreaElement)?.value?.trim() || '';
			const name    = (document.getElementById('memory-name')    as HTMLInputElement)?.value?.trim()    || '';
			const submitBtn = document.getElementById('share-memories-submit') as HTMLButtonElement;
			submitBtn.disabled = true;
			try {
				const { base64, contentType } = await compressImage(file, MAX_IMAGE_SIZE, MAX_DIMENSION);
				if (!base64) { errorEl.textContent = 'Photo too large — try a smaller image.'; errorEl.hidden = false; return; }
				const res  = await fetch(SUBMIT_URL, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ imageBase64: base64, contentType, caption, name }) });
				const data = await res.json().catch(() => ({}));
				if (!res.ok) {
					errorEl.textContent = res.status === 404 ? 'Run "npm run dev:netlify" for local testing.' : ((data?.error) ? String(data.error) : `Error ${res.status}`);
					errorEl.hidden = false; return;
				}
				successEl.hidden = false;
				await renderMemories();
				setTimeout(closeShareModal, 1500);
			} catch (err) {
				errorEl.textContent = err instanceof Error ? err.message : 'Something went wrong.';
				errorEl.hidden = false;
			} finally { submitBtn.disabled = false; }
		});

		async function compressImage(file: File, maxBytes: number, maxDim: number) {
			const contentType = 'image/jpeg';
			if (!file.type.startsWith('image/')) return { base64: null, contentType };
			const img = await new Promise<HTMLImageElement>((res, rej) => {
				const url = URL.createObjectURL(file), i = new Image();
				i.onload  = () => { URL.revokeObjectURL(url); res(i); };
				i.onerror = () => { URL.revokeObjectURL(url); rej(new Error('load')); };
				i.src = url;
			});
			const scale  = Math.min(1, maxDim / Math.max(img.width, img.height));
			const canvas = Object.assign(document.createElement('canvas'), { width: Math.round(img.width * scale), height: Math.round(img.height * scale) });
			canvas.getContext('2d')!.drawImage(img, 0, 0, canvas.width, canvas.height);
			for (const q of [0.85, 0.7, 0.55, 0.4, 0.25]) {
				const blob = await new Promise<Blob | null>((r) => canvas.toBlob(r, 'image/jpeg', q));
				if (blob && blob.size <= maxBytes) {
					const base64 = await new Promise<string>((res, rej) => {
						const reader = new FileReader();
						reader.onload  = () => res((reader.result as string).split(',')[1] || '');
						reader.onerror = rej;
						reader.readAsDataURL(blob);
					});
					return { base64, contentType };
				}
			}
			return { base64: null, contentType };
		}

		renderMemories();
	</script>
</BaseLayout>

<style>
	/* ── Page base ─────────────────────────────────────────────── */
	.hero { color: var(--color-cream); padding: 4rem 2rem; text-align: center; }
	.wave-wrap { position: relative; overflow: visible; min-height: 120px; }
	.wave-wrap :global(.wave-surfer-wrap) { transition: opacity 0.4s ease, visibility 0.4s ease; }
	:global(.wave-wrap.pirate-mode) :global(.wave-surfer-wrap) { opacity: 0; visibility: hidden; pointer-events: none; }
	.hero-content { max-width: 700px; margin: 0 auto; }
	.hero h1 { font-family: var(--font-display); font-size: clamp(2.5rem, 5vw, 4rem); font-weight: 600; letter-spacing: 0.02em; margin-bottom: 1rem; line-height: 1.2; }
	.hero-title   { animation: hero-fade-up 1s cubic-bezier(0.22,1,0.36,1) forwards; }
	.hero-tagline { opacity: 0; animation: hero-fade-up 0.8s cubic-bezier(0.22,1,0.36,1) 0.2s forwards; }
	@keyframes hero-fade-up { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:translateY(0); } }
	.tagline { font-size: 1.25rem; opacity: 0.95; font-weight: 400; }

	.reunion-content {
		position: relative; z-index: 1;
		padding: 4rem 2rem 3rem;
		background: linear-gradient(180deg, var(--color-sand) 0%, var(--color-foam) 100%);
		box-shadow: inset 0 1px 0 rgba(255,255,255,0.4);
	}
	.reunion-content .container { max-width: 820px; margin: 0 auto; }

	.reunion-intro-body {
		margin-bottom: 0.5rem;
	}
	.reunion-img-wrap {
		position: relative;
		width: 80%;
		margin: 1.5rem auto 2.5rem;
		padding: 14px 14px 44px;
		background: #fff;
		box-shadow: 0 12px 32px rgba(0,0,0,0.13), 0 4px 12px rgba(0,0,0,0.08);
		transition: transform 0.35s ease, box-shadow 0.35s ease;
	}

	.reunion-img-wrap:nth-child(odd)  { transform: rotate(-1.5deg); }
	.reunion-img-wrap:nth-child(even) { transform: rotate(1deg); }
	.reunion-img-wrap:hover { transform: rotate(0deg) scale(1.01); box-shadow: 0 18px 40px rgba(0,0,0,0.16); }

	.reunion-img-wrap .tape-element {
		position: absolute;
		top: -14px;
		left: 50%;
		transform: translateX(-50%) rotate(-2deg);
		width: 100px;
		height: 30px;
		background: rgba(184, 220, 228, 0.55);
		backdrop-filter: blur(2px);
		box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
		z-index: 2;
	}

	.reunion-section-img {
		display: block;
		width: 100%;
		height: auto;
	}
	.reunion-intro   { font-family: var(--font-body); font-size: 1.0625rem; color: var(--color-ink); line-height: 1.75; margin: 0 0 1rem; }
	.reunion-intro-body > .reunion-intro:first-child { font-size: 1.25rem; line-height: 1.7; }
	.reunion-intro:last-of-type { margin-bottom: 0; }
	.reunion-subhead { font-family: var(--font-body); font-size: clamp(1rem,2.5vw,1.2rem); font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--color-ink); margin: 2rem 0 0.75rem; line-height: 1.4; }
	.reunion-lead    { font-family: var(--font-body); font-size: 1rem; color: var(--color-ink); line-height: 1.6; margin: 0 0 0.75rem; }
	.reunion-list    { font-family: var(--font-body); font-size: 1rem; color: var(--color-ink); line-height: 1.75; margin: 0 0 1.5rem; padding-left: 1.5rem; list-style: disc; }
	.reunion-list li { margin-bottom: 0.35rem; }
	.reunion-body    { font-family: var(--font-body); font-size: 1.0625rem; color: var(--color-ink); line-height: 1.75; margin: 0; }

	.doyle-shell {
		width: 100%;
		max-width: 72rem;
		margin: 3rem auto 2.5rem;
		padding: 0 1.5rem;
		box-sizing: border-box;
	}
	.doyle-remember {
		display: flex;
		flex-direction: column;
		align-items: stretch;
		gap: 2rem;
		max-width: 65ch;
		width: 100%;
		margin: 0 auto;
		padding: 0;
	}
	.doyle-figure {
		margin: 0;
		width: 100%;
		max-width: 100%;
	}
	.doyle-img-wrap {
		position: relative;
		width: 100%;
		margin: 0 0 0.75rem;
		padding: 14px 14px 44px;
		background: #fff;
		box-shadow: 0 12px 32px rgba(0,0,0,0.13), 0 4px 12px rgba(0,0,0,0.08);
		transform: rotate(-1.5deg);
		transition: transform 0.35s ease, box-shadow 0.35s ease;
	}
	.doyle-img-wrap:hover {
		transform: rotate(0deg) scale(1.01);
		box-shadow: 0 18px 40px rgba(0,0,0,0.16);
	}
	.doyle-img-wrap .tape-element {
		position: absolute;
		top: -14px;
		left: 50%;
		transform: translateX(-50%) rotate(-2deg);
		width: 100px;
		height: 30px;
		background: rgba(184, 220, 228, 0.55);
		backdrop-filter: blur(2px);
		box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
		z-index: 2;
	}
	.doyle-photo {
		display: block;
		width: 100%;
		height: auto;
	}
	.doyle-caption {
		font-family: var(--font-body);
		font-size: 0.875rem;
		font-style: italic;
		color: var(--color-ink);
		line-height: 1.45;
		margin: 0;
		text-align: left;
	}
	.doyle-heading-block {
		text-align: left;
		margin: 0;
		padding: 0;
		background: none;
		color: inherit;
	}
	.doyle-title {
		font-family: var(--font-display);
		font-size: clamp(1.85rem, 3.5vw, 2.5rem);
		font-weight: 600;
		color: var(--color-ink);
		line-height: 1.15;
		margin: 0 0 0.5rem;
	}
	.doyle-subtitle {
		font-family: var(--font-body);
		font-size: 0.85rem;
		font-weight: 700;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: var(--color-ink);
		opacity: 0.75;
		margin: 0;
	}
	.doyle-body {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
	.doyle-body p {
		font-family: var(--font-body);
		font-size: 1.02rem;
		color: var(--color-ink);
		line-height: 1.7;
		margin: 0;
	}
	.doyle-signoff {
		font-family: var(--font-display) !important;
		font-size: 1.3rem !important;
		font-weight: 600;
		font-style: italic;
		text-align: right;
		margin: 0.5rem 0 0 !important;
	}

	.event-card {
		background: rgba(184,220,228,0.5); backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
		padding: 1.5rem 1.5rem 1.5rem 1.25rem; border-radius: 12px;
		border: 1px solid rgba(255,255,255,0.5); border-left: 6px solid var(--color-accent);
		box-shadow: 0 4px 20px rgba(0,0,0,0.04); transition: transform 0.3s ease, box-shadow 0.3s ease; margin-bottom: 1.25rem;
	}
	.event-card:hover { transform: translateX(10px); box-shadow: 0 8px 28px rgba(0,0,0,0.08); }
	.event-card h3, .event-card h2 { font-family: var(--font-display); font-size: 1.2rem; font-weight: 600; color: var(--color-ink); margin: 0 0 0.6rem; line-height: 1.3; }
	.event-card p  { font-family: var(--font-body); font-size: 1rem; color: var(--color-ink); line-height: 1.7; margin: 0; }
	.event-card-cta { text-align: center; border-left-color: var(--wave-deep); background: rgba(26,95,122,0.08); margin-top: 2rem; }
	.reunion-cta-heading { font-family: var(--font-body) !important; font-size: clamp(0.95rem,2.2vw,1.1rem) !important; font-weight: 700 !important; letter-spacing: 0.06em; text-transform: uppercase; color: var(--color-ink); line-height: 1.45; margin: 0 0 0.75rem !important; }
	.reunion-cta-body    { font-family: var(--font-body); font-size: 1rem; color: var(--color-ink); line-height: 1.7; margin: 0 0 1.25rem; }

	.reunion-share-btn { display: inline-block; font-family: var(--font-body); font-size: 0.9rem; font-weight: 600; color: var(--wave-deep); background: transparent; padding: 0.5rem 1.25rem; border: 2px solid var(--wave-mid); border-radius: 50px; cursor: pointer; transition: background 0.25s ease, color 0.25s ease; }
	.reunion-share-btn:hover { background: var(--wave-deep); color: var(--color-cream); }
	.reunion-memories-heading { margin-top: 3rem; }

	.shared-memories-container { width: 100%; margin-top: 1.5rem; }
	.shared-memories-msg { font-family: var(--font-body); color: var(--color-sepia); font-style: italic; text-align: center; padding: 2rem; margin: 0; }
	.shared-memories-err { font-style: normal; color: #b91c1c; }

	/* ── Reels-style shared memories ──────────────────────────── */

	:global(.tt-outer) {
		display: flex;
		justify-content: center;
		padding: 0 1rem;
	}

	:global(.tt-feed) {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
		width: 100%;
		max-width: 420px;
	}

	:global(.reel-nav) {
		display: flex;
		justify-content: center;
		gap: 1rem;
		width: 100%;
	}

	:global(.reel-nav-btn) {
		flex: 1;
		max-width: 160px;
		font-family: var(--font-body);
		font-size: 0.9rem;
		font-weight: 600;
		color: var(--wave-deep);
		background: transparent;
		padding: 0.65rem 1.25rem;
		border: 2px solid var(--wave-mid);
		border-radius: 50px;
		cursor: pointer;
		transition: background 0.25s ease, color 0.25s ease, opacity 0.25s ease;
	}

	:global(.reel-nav-btn:hover:not(:disabled)) {
		background: var(--wave-deep);
		color: var(--color-cream);
	}

	:global(.reel-nav-btn:disabled) {
		opacity: 0.4;
		cursor: not-allowed;
	}

	:global(.tt-card) {
		width: 100%;
		max-width: 420px;
		height: clamp(520px, 82vh, 760px);
		position: relative;
		background: #111;
		border-radius: 18px;
		overflow: hidden;
		box-shadow: 0 16px 48px rgba(0,0,0,0.3), 0 4px 16px rgba(0,0,0,0.2);
	}

	:global(.tt-stage) {
		position: absolute; inset: 0; overflow: hidden;
	}

	/* Reel slide */
	:global(.reel-slide) {
		position: absolute; inset: 0;
		display: flex; flex-direction: column;
		cursor: default;
		user-select: none;
	}

	/* Full-cover photo */
	:global(.reel-photo) {
		position: absolute; inset: 0;
		width: 100%; height: 100%;
		object-fit: cover; display: block;
		pointer-events: none;
	}

	/* Bottom gradient overlay */
	:global(.reel-gradient) {
		position: absolute; inset: 0;
		background: linear-gradient(to top, rgba(0,0,0,0.78) 0%, rgba(0,0,0,0.28) 45%, transparent 70%);
		pointer-events: none;
	}

	/* Bottom-left info */
	:global(.reel-info) {
		position: absolute; bottom: 80px; left: 12px; right: 90px;
		z-index: 5; color: #fff;
		text-shadow: 0 1px 4px rgba(0,0,0,0.65);
		font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
	}
	:global(.reel-username) { font-weight: 700; font-size: 0.9rem; margin: 0 0 4px; }
	:global(.reel-caption) {
		font-size: 0.875rem; line-height: 1.45; margin: 0 0 3px;
		display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden;
	}
	:global(.reel-by) { font-size: 0.8125rem; font-style: italic; opacity: 0.85; margin: 0 0 4px; }
	:global(.reel-swipe-hint) {
		font-size: 0.72rem; opacity: 0.55; margin: 2px 0 0;
		animation: hint-pulse 2.8s ease-in-out infinite;
	}
	@keyframes hint-pulse {
		0%, 100% { opacity: 0.55; }
		50%       { opacity: 0.15; }
	}

	/* Right sidebar */
	:global(.reel-sidebar) {
		position: absolute; right: 10px; bottom: 80px;
		display: flex; flex-direction: column; align-items: center;
		gap: 20px; z-index: 5;
		pointer-events: auto;
		transition: opacity 0.2s ease;
	}
	/* Keep sidebar under the comments sheet so icons don't sit on top of it */
	:global(.reel-slide:has(.reel-comments-panel.open) .reel-sidebar) {
		opacity: 0;
		pointer-events: none;
	}
	:global(.reel-cmt-btn) {
		background: none; border: none; cursor: pointer;
		display: flex; flex-direction: column; align-items: center;
		gap: 3px; color: #fff; font-size: 0.7rem; font-weight: 600;
		padding: 0; line-height: 1;
		text-shadow: 0 1px 3px rgba(0,0,0,0.5);
		transition: opacity 0.15s;
	}
	:global(.reel-cmt-btn:hover) { opacity: 0.75; }
	:global(.reel-cmt-btn span) { font-size: 0.7rem; line-height: 1; }
	:global(.reel-save-btn) {
		display: flex; flex-direction: column; align-items: center;
		gap: 3px; color: #fff; text-decoration: none;
		font-size: 0.7rem; font-weight: 600;
		text-shadow: 0 1px 3px rgba(0,0,0,0.5);
		transition: opacity 0.15s;
	}
	:global(.reel-save-btn:hover) { opacity: 0.75; }
	:global(.reel-counter) { font-size: 0.7rem; font-weight: 700; color: rgba(255,255,255,0.85); line-height: 1; }
	:global(.reel-counter span) { font-weight: 400; opacity: 0.65; }

	/* Comments bottom sheet */
	:global(.reel-comments-panel) {
		position: absolute; inset: 0; z-index: 30;
		pointer-events: none; opacity: 0;
		transition: opacity 0.25s;
	}
	:global(.reel-comments-panel.open) {
		pointer-events: auto; opacity: 1;
	}
	:global(.reel-cmts-backdrop) {
		position: absolute; inset: 0;
		background: rgba(0,0,0,0.45);
		border: none; cursor: pointer; z-index: 0;
		width: 100%; height: 100%;
	}
	:global(.reel-cmts-sheet) {
		position: absolute; bottom: 0; left: 0; right: 0;
		background: #1a1a1a; border-radius: 20px 20px 0 0;
		max-height: 65%; display: flex; flex-direction: column;
		transform: translateY(100%);
		transition: transform 0.32s cubic-bezier(0.4,0,0.2,1);
		z-index: 1;
	}
	:global(.reel-comments-panel.open .reel-cmts-sheet) {
		transform: translateY(0);
	}
	:global(.reel-cmts-handle) {
		width: 36px; height: 4px; border-radius: 2px;
		background: rgba(255,255,255,0.25); margin: 10px auto 0; flex-shrink: 0;
	}
	:global(.reel-cmts-header) {
		display: flex; align-items: center;
		padding: 10px 16px 8px;
		border-bottom: 1px solid rgba(255,255,255,0.1);
		flex-shrink: 0;
	}
	:global(.reel-cmts-title) {
		flex: 1; font-size: 0.875rem; font-weight: 700; color: #fff;
		font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
	}
	:global(.tt-comment-count) { font-weight: 400; }
	:global(.reel-cmts-close) {
		background: none; border: none; cursor: pointer;
		font-size: 1rem; color: rgba(255,255,255,0.7);
		padding: 4px 6px; border-radius: 4px;
		transition: color 0.15s;
	}
	:global(.reel-cmts-close:hover) { color: #fff; }

	/* Comments list */
	:global(.tt-comments-list) {
		flex: 1; overflow-y: auto; padding: 8px 16px;
		display: flex; flex-direction: column; gap: 8px; scroll-behavior: smooth;
	}
	:global(.tt-comment-placeholder) {
		font-size: 0.8125rem; color: rgba(255,255,255,0.45); font-style: italic; margin: 0;
	}
	:global(.tt-comment) {
		font-size: 0.875rem; line-height: 1.45;
		display: flex; gap: 6px; flex-wrap: wrap; color: #fff;
	}
	:global(.tt-cmt-name) { font-weight: 700; flex-shrink: 0; color: rgba(255,255,255,0.9); }

	/* Comment form */
	:global(.reel-cmt-form) {
		border-top: 1px solid rgba(255,255,255,0.1);
		padding: 8px 12px 14px; display: flex; flex-direction: column;
		gap: 5px; flex-shrink: 0;
	}
	:global(.reel-cmt-row) { display: flex; align-items: center; gap: 6px; }
	:global(.reel-cmt-input) {
		flex: 1; background: rgba(255,255,255,0.1);
		border: 1px solid rgba(255,255,255,0.2); border-radius: 20px;
		color: #fff; font-size: 0.875rem; padding: 6px 12px; outline: none;
		font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
	}
	:global(.reel-cmt-input::placeholder) { color: rgba(255,255,255,0.38); }
	:global(.reel-name-input) {
		background: transparent; border: none;
		border-bottom: 1px solid rgba(255,255,255,0.15);
		color: rgba(255,255,255,0.6); font-size: 0.8125rem;
		padding: 3px 4px; outline: none; width: 100%;
		font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
	}
	:global(.reel-name-input::placeholder) { color: rgba(255,255,255,0.28); }
	:global(.ig-cmt-post) {
		background: none; border: none; cursor: pointer;
		font-size: 0.875rem; font-weight: 700;
		color: #0095f6; padding: 0; white-space: nowrap;
		font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
		transition: opacity 0.15s;
	}
	:global(.ig-cmt-post:hover:not(:disabled)) { opacity: 0.7; }
	:global(.ig-cmt-post:disabled) { opacity: 0.4; cursor: not-allowed; }
	:global(.reel-cmt-error) { font-size: 0.75rem; color: #e0245e; margin: 0; }

	/* Mobile */
	@media (max-width: 640px) {
		:global(.tt-outer) { padding: 0; }
		:global(.tt-card)  { border-radius: 0; }
		:global(.reel-info)    { bottom: 90px; right: 80px; }
		:global(.reel-sidebar) { bottom: 90px; }
	}

	/* ── Share memories modal ──────────────────────────────────── */
	.share-memories-modal { position: fixed; inset: 0; z-index: 9999; display: flex; align-items: center; justify-content: center; padding: 1rem; }
	.share-memories-modal[hidden] { display: none; }
	.share-memories-overlay { position: absolute; inset: 0; background: rgba(0,0,0,0.5); }
	.share-memories-box { position: relative; background: var(--color-cream); border-radius: 12px; padding: 1.5rem 1.75rem; box-shadow: 0 12px 40px rgba(0,0,0,0.2); min-width: 280px; max-width: 420px; width: 100%; }
	.share-memories-title  { font-family: var(--font-display); font-size: 1.35rem; font-weight: 600; color: var(--color-ink); margin: 0 0 1.25rem; }
	.share-memories-form   { display: flex; flex-direction: column; gap: 0.75rem; }
	.share-memories-label  { font-family: var(--font-body); font-size: 0.9rem; font-weight: 600; color: var(--color-ink); }
	.share-memories-file, .share-memories-input, .share-memories-textarea { font-family: var(--font-body); font-size: 1rem; padding: 0.5rem 0.75rem; border: 1px solid var(--color-sand-dark); border-radius: 6px; background: var(--color-foam); }
	.share-memories-textarea { resize: vertical; min-height: 4rem; }
	.share-memories-error   { font-size: 0.9rem; color: #b91c1c; margin: 0; }
	.share-memories-success { font-size: 0.9rem; color: #15803d; margin: 0; }
	.share-memories-actions { display: flex; gap: 0.75rem; justify-content: flex-end; margin-top: 0.5rem; }
	.share-memories-btn { font-family: var(--font-body); font-size: 0.9rem; font-weight: 600; padding: 0.5rem 1.25rem; border-radius: 6px; cursor: pointer; border: 1px solid transparent; transition: background 0.2s ease; }
	.share-memories-cancel  { background: transparent; color: var(--color-ink); border-color: var(--color-sand-dark); }
	.share-memories-submit  { background: var(--wave-deep); color: var(--color-cream); }
	.share-memories-submit:hover:not(:disabled) { background: var(--wave-mid); }
	.share-memories-submit:disabled { opacity: 0.7; cursor: not-allowed; }

	@media (max-width: 768px) {
		.hero { padding: 3rem 1rem; }
		.hero .hero-title { font-size: clamp(1.75rem, 6vw, 2.25rem); }
		.reunion-content { padding: 2.5rem 1rem 2rem; }
		.share-memories-cancel, .share-memories-submit { min-height: 48px; padding: 0.85rem 1.25rem; }
		.reunion-share-btn { min-height: 44px; padding: 0.6rem 1.5rem; }
		.doyle-shell { padding: 0 1rem; }
	}

	@media (max-width: 640px) {
		.reunion-intro-grid { grid-template-columns: 1fr; gap: 1.25rem; }
		:global(.reel-cmt-btn) { min-width: 44px; min-height: 44px; justify-content: center; }
		:global(.reel-info) { font-size: 0.85rem; }
		.share-memories-box { padding: 1.25rem 1rem; }
	}
</style>
